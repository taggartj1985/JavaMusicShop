package instruments.strings;

import instruments.Instrument;

public class Guitar extends Instrument {
    public Guitar(String model, String make, String colour, String type, double buyInCost, double sellingPrice){
        super(model, make, colour, type, buyInCost, sellingPrice);
    }
}
